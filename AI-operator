conv: 卷积操作符。
relu: 激活函数。只允许正数通过，丢弃负数。
maxpool: CNN中普遍使用的下采样或池化操作，主要用于减少特征图的尺寸(高度和宽度)，减少数据量已提高计算效率，帮助提取更为重要的特征并增加模型的空间不变性。
averagepool: 和maxpool功能类似，主要抑制过拟合，降低计算成本，更光滑的特征表达，保留背景信息。
cancat: 将多个张量在指定维度拼接起来。

