# ssh隧道
ssh -CNg -L 6006:127.0.0.1:6006 root@123.125.240.150 -p 42151

# 连接复用
vim ~/.ssh/config
# content
Host *
    ControlMaster auto
    ControlPath ~/.ssh/sockets/%r@%h:%p
    ControlPersist 10m

mkdir -p ~/.ssh/sockets

# ssh跳板机
ssh -J [跳板机用户]@[跳板机IP]:[端口] [目标用户]:[目标IP]:[目标端口]
可以写配置文件简单连接到目标主机
